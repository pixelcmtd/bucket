all {
    !go build
}

test {
    !mkdir -p testdir
    !go run bucket.go testdir
}

release {
    !GOOS=linux GOARCH=amd64 go build -o bucket-x86 -v -a
    !sha256sum bucket-*
}

clean {
    !rm -f bucket bucket-*
}
